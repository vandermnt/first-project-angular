import { MinimumValidatorDirective } from './minimum-validator.directive';

describe('MinimumValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinimumValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
